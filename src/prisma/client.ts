// Barrel re-export ของ generated Prisma client — ไฟล์อื่นทั้งหมดควร import
// PrismaClient/AuthProvider/User ผ่านไฟล์นี้แทนการ import ตรงจาก
// generated/prisma เพื่อให้แก้ path เดียวถ้า output ของ generator เปลี่ยน
export * from '../generated/prisma/client.js';
